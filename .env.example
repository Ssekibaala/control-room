# Copy this file to .env for local testing (never commit the real .env).
# On Render, set these as real Environment Variables in the dashboard,
# not in a file, Render injects them at runtime.

# ---- Flask ----
FLASK_SECRET_KEY=replace-with-a-long-random-string

# ---- Mailbox (mail.teletracfleets.com, standard IMAPS) ----
EMAIL_ADDRESS=brandon.b@teletracfleets.com
EMAIL_PASSWORD=the-real-account-password
# This account should ONLY be used for these report subscriptions,
# never a general inbox, since this host doesn't support scoped
# app-passwords, only the real account password.

# ---- Import endpoint security ----
# Generate any long random string, put the SAME value in the Apps
# Script scheduler (see apps_script_scheduler.gs). Whoever calls
# /api/import must send this exact value in the X-API-Key header.
IMPORT_API_KEY=replace-with-a-long-random-string

# ---- Google Sheets (feedback/history storage) ----
# See the setup steps at the top of sheets_store.py for how to get
# these two values. GOOGLE_SERVICE_ACCOUNT_JSON is the *entire*
# downloaded JSON key file, pasted as one line.
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...", ...}
FEEDBACK_SHEET_ID=the-id-from-your-google-sheet-url
