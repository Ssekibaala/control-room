Flask==3.1.3
openpyxl>=3.1
requests>=2.31
gunicorn>=22.0
python-dotenv>=1.0
# Already a transitive Flask dependency; pinned explicitly because
# app.py now imports it directly to sign email response links.
itsdangerous>=2.1

# Google Sheets support (feedback/history storage)
gspread>=6.1
google-auth>=2.30

# Outbound email (mailer.py) - HTTP API, not SMTP, because Render blocks
# outbound SMTP ports on free web services.
resend>=2.0

# Only needed if you run the test suite yourself
# (not required in production on Render)
